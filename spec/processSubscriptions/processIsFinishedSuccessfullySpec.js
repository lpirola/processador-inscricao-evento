describe('Processing subscriptions is finished with success', () => {
		it('Given E-mail is configured and could be send')
		it('And Datasource is configured, acessible and not empty')
		it('And Filters are not empty and validated')
		it('And Actions are not empty and validated')
		it('When Developer try to process all subscriptions')
		it('Then Datasource must be all processed')
		it('And output a message with all success actions executed')
})
