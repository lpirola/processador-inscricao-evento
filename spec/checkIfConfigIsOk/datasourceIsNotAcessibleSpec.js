import mainCommand from '../../src'

describe('Given E-mail is configured and could be send', () => {
	it('And Datasource config is valid')
	it('When Developer try to check if configs are')
	it('Then Process should stop')
	it('And output a message with error details')
});
